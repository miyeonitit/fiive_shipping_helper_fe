// import AxiosInstance from './AxiosHandler'

// type bodyType = {
//   shippingNumber: string[]
// }

// export const GetFetcher = (url: string) =>
//   AxiosInstance.get(url).then((res) => res.data)

// export const PostFetcher = (url: string, body: bodyType) =>
//   AxiosInstance.post(url).then((res) => res.data, body)

// export const DeleteFetcher = (url: string) =>
//   AxiosInstance.delete(url).then((res) => res.data)
